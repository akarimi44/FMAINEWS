{% extends 'base.html.twig' %}

{% block title %}Contacts index{% endblock %}

{% block body %}
<div class="background-image d-flex justify-content-center align-items-start pt-5">
    <div class="container bg-light bg-opacity-75 rounded p-4 shadow">
    <h1>Contacts index</h1>

    <table class="responsive-table w-100">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Sujet</th>
                <th>Message_contact</th>
                <th>Email_contact</th>
                <th>Created_at_contact</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for contact in contacts %}
            <tr>
                <td>{{ contact.id }}</td>
                <td>{{ contact.title }}</td>
                <td>{{ contact.sujet }}</td>
                <td>{{ contact.messageContact }}</td>
                <td>{{ contact.emailContact }}</td>
                <td>{{ contact.createdAtContact ? contact.createdAtContact|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_contacts_show', {'id': contact.id}) }}" class="btn btn-sm btn-secondary w-100 mb-1">Détails</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    </div>
    
</div>
{% endblock %}
