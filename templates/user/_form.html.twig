<div class="d-flex justify-content-center pt-5">
    <div class="card p-4 shadow-lg" style="width: 35rem;">
        {{ form_start(form) }}
            <h2 class="card-title text-center mb-4">
                    Modifier l'utilisateur
            </h2>
            <a href="{{ path('app_user_index') }}" class="btn btn-secondary">Retourner</a>
            <div class="form-group mb-3">
                {{ form_label(form.roles, 'Rôles', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.roles, {'attr': {'class': 'form-select'}}) }}
                {{ form_errors(form.roles) }}
            </div>
            
            <div class="form-group form-check mb-4 d-flex align-items-center">
                {{ form_widget(form.opption, {'attr': {'class': 'form-check-input'}}) }}
                {{ form_label(form.opption, 'Activer l\'option', {'label_attr': {'class': 'form-check-label ms-2'}}) }}
                {{ form_errors(form.opption) }}
            </div>

            <button type="submit" class="btn btn-primary w-100">{{ button_label|default('Enregistrer') }}</button>
        {{ form_end(form) }}
    </div>
</div>