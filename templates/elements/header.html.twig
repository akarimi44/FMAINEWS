<nav class="navbar navbar-expand-lg navbar-black bg-black text-white">
  <div class="container-fluid">
    <a class="nameSite text-white" href="/">Fmai News</a>
    <button class="navbar-toggler bg-white" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse sideNav" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link text-white" aria-current="page" href="/">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link  text-white" href="/contacts/new">Contactez-nous</a>
        </li>
         <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle text-white" href="#" id="navbarCategoryDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Catégories
          </a>
          <ul class="dropdown-menu" aria-labelledby="navbarCategoryDropdown">
            <li><a class="dropdown-item" href="{{path ('app_posts_by_category', {id: 1})}}">Actualités</a></li>
            <li><a class="dropdown-item" href="{{path ('app_posts_by_category', {id: 2})}}">Cybersécurité</a></li>
            <li><a class="dropdown-item" href="{{path ('app_posts_by_category', {id: 3})}}">Développement</a></li>
            <li><a class="dropdown-item" href="{{path ('app_posts_by_category', {id: 4})}}">Economie</a></li>
            <li><a class="dropdown-item" href="{{path ('app_posts_by_category', {id: 5})}}">Société</a></li>
          </ul>
        </li>
      </ul>
      <div class="header-social-links">
        {% if app.user %}
            {% if app.user.opption %}
                {% if app.user.avatar is empty %}
                    <i class="fa-solid fa-circle-user" style="color: white"></i>
                    <span class="zxfirstname" style="text-decoration:none;">{{ app.user.pseudo|capitalize }}</span>
                    <a class="loggouts mr-2" href="{{ path('app_logout') }}">
                        <i class="fa solid fa-power-off" style="color: white"></i>
                    </a>
                    <a class="zxadmin mr-2" href="{{ path('admin_dashboard') }}">
                        <i class="fa fa-circle-play" style="color: white"></i>
                    </a>
                {% else %}
                    <img class="roundedImage" src="{{ asset('uploads/images/' ~ app.user.avatar) }}" style="width:25px; height:25px; border-radius:50%">&ensp;
                    <span class="zxfirstname">{{ app.user.pseudo|capitalize }}</span>
                    <a class="loggouts" href="{{ path('app_logout') }}">&ensp;<i class="fa-solid fa-power-off" style="color: white"></i></a>
                    <a class="zxadmin mr-2" href="{{ path('admin_dashboard') }}">
                        <i class="fa fa-circle-play" style="color: white"></i>
                    </a>
                {% endif %}
            {% else %}
                {% if app.user.avatar is empty %}
                    <i class="fa-solid fa-circle-user" style="color: black"></i>
                    <span class="zxfirstname" style="text-decoration:none;">{{ app.user.pseudo|capitalize }}</span>
                    <a class="loggouts mr-2" href="{{ path('app_logout') }}">
                        <i class="fa solid fa-power-off" style="color: white"></i>
                    </a>
                {% else %}
                    <img class="roundedImage" src="{{ asset('uploads/images/' ~ app.user.avatar) }}" style="width:25px; height:25px; border-radius:50%">&ensp;
                    <span class="zxfirstname">{{ app.user.pseudo|capitalize }}</span>
                    <a class="loggouts" href="{{ path('app_logout') }}">&ensp;<i class="fa-solid fa-power-off" style="color:white"></i></a>
                {% endif %}
            {% endif %}
        {% else %}
            <a class="login" href="{{ path('app_login') }}">
                <i class="fa-regular fa-circle-user" style="color: white"></i>&ensp;
            </a>
        {% endif %}
      </div>
    </div>
  </div>
</nav>